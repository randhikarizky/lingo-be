export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: "sans-serif" }}>
      <h1>Lingora API</h1>
      <p>
        Backend service aktif. Gunakan endpoint di <code>/api/v1</code>.
      </p>
    </main>
  );
}
